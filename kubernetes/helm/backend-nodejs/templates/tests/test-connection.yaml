apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-nodejs.fullname" . }}-test-connection"
  labels:
    {{- include "backend-nodejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-nodejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
